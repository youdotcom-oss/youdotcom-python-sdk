"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .agentrunsresponsewebsearchresult import (
    AgentRunsResponseWebSearchResult,
    AgentRunsResponseWebSearchResultTypedDict,
)
from enum import Enum
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict
from youdotcom.types import BaseModel


class Type(str, Enum):
    r"""The type of output. This can either be:
    * `message.answer` for text responses
    * `web_search.results` for output that contains web links. `web_search.results` only appear when you use the `research` tool or express agent with web_search
    """

    MESSAGE_ANSWER = "message.answer"
    WEB_SEARCH_RESULTS = "web_search.results"


class AgentRunsResponseOutputTypedDict(TypedDict):
    r"""The response populated by the agent."""

    type: Type
    r"""The type of output. This can either be:
    * `message.answer` for text responses
    * `web_search.results` for output that contains web links. `web_search.results` only appear when you use the `research` tool or express agent with web_search
    """
    text: NotRequired[str]
    r"""The text response of the agent. This field returns when `type == message.answer`. The response returns as markdown formatted text.

    For an overview of Markdown syntax, see the [Basic Syntax Markdown Guide](https://www.markdownguide.org/basic-syntax/)
    """
    content: NotRequired[List[AgentRunsResponseWebSearchResultTypedDict]]
    r"""The text response of the agent.
    This field returns when `type == web_search.results`
    """


class AgentRunsResponseOutput(BaseModel):
    r"""The response populated by the agent."""

    type: Type
    r"""The type of output. This can either be:
    * `message.answer` for text responses
    * `web_search.results` for output that contains web links. `web_search.results` only appear when you use the `research` tool or express agent with web_search
    """

    text: Optional[str] = None
    r"""The text response of the agent. This field returns when `type == message.answer`. The response returns as markdown formatted text.

    For an overview of Markdown syntax, see the [Basic Syntax Markdown Guide](https://www.markdownguide.org/basic-syntax/)
    """

    content: Optional[List[AgentRunsResponseWebSearchResult]] = None
    r"""The text response of the agent.
    This field returns when `type == web_search.results`
    """
